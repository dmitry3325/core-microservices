server:
  port: ${TRANSLATION-SERVICE-PORT:3003}

spring:
  application:
    name: TRANSLATION-SERVICE
  main:
    banner-mode: off
  jackson:
    default-property-inclusion: non_null
  config:
    import: classpath:db-config.yaml
