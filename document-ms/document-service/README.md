# Document Service (document-ms)

Local development:
- Uses Postgres and MinIO for storage. A `docker-compose` in repository `docker/` can be used to run these services.
- Set environment variables from `.env-example`.

Phase 1: API & skeleton created. Next: implement entities and repositories (Phase 2).

